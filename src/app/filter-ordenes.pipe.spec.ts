import { FilterOrdenesPipe } from './filter-ordenes.pipe';

describe('FilterOrdenesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrdenesPipe();
    expect(pipe).toBeTruthy();
  });
});
